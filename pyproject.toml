[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pose_dynamics"
version = "0.1.0"
description = "Pose dynamics analysis toolkit"
authors = [
    { name = "Carter Sale" }
]
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
