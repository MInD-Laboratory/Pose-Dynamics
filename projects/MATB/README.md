# MATB Case Study

This directory contains the MATB case study.
All analysis and statistical modeling live in the submodule:

- `projects/MATB/Measuring_Workload_Dynamics_in_OpenMATB`

Use that submodule (especially `Pose/`) for full methods, feature generation, model fitting, and statistical details.

This notebook (`MATB.ipynb`) is figure-only: it reads precomputed outputs and reproduces the MATB case-study visualizations.
